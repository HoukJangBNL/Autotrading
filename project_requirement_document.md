# 자동 주식 매매 프로그램 PRD (Charles Schwab API 기반)

## 개요 (Overview)

이 프로젝트의 목표는 Charles Schwab의 API를 활용하여 자동 주식 매매 프로그램을 개발하는 것입니다. Python 언어로 작성하며, Charles Schwab API에 대한 비공식 Python 래퍼 라이브러리인 `schwab-py`를 사용합니다¹ ².

`schwab-py` 라이브러리는 OAuth 로그인/인증부터 시세 조회, 과거 가격 데이터, 스트리밍 실시간 시세, 주문 실행 및 계좌 정보 조회까지 광범위한 기능을 제공합니다¹ ². 프로그램은 1분봉 데이터를 기반으로 자동거래를 수행하며, 3가지 모드 (종목 발굴, 종목 선정, 실시간 거래)로 동작하게 됩니다. 또한 전략을 모듈화하여 여러 전략을 플러그인처럼 추가 가능하게 하고, 백테스팅과 AI 기반 최적화를 통해 거래 변수(파라미터)를 자동으로 튜닝하는 기능을 포함합니다. 최종적으로 사용자에게 데스크탑 GUI를 제공하여 실시간 모니터링과 제어, 그리고 일별 리포트 확인이 가능하도록 설계합니다.

## Charles Schwab API 연동 기능 (Schwab API Integration)

본 시스템은 Schwab에서 제공하는 공식 개발자 API를 활용합니다. Schwab API는 OAuth2 인증을 사용하며 schwab-py가 이를 안전하게 처리해주므로, .env에 저장된 API 키/시크릿 등을 이용하여 토큰을 발급받습니다. (Schwab API 토큰은 7일로 유효기간이 짧기 때문에 주기적 재인증이 필요합니다³ ⁴.) schwab-py를 통해 Schwab API의 다양한 엔드포인트에 접근할 수 있는데, 주요 기능은 다음과 같습니다:

### 시세 데이터 조회
개별 종목 현재가, 호가, 거래량 등의 실시간 시세(Level 1) 조회와 과거 시세 히스토리컬 데이터 조회가 가능합니다¹. 예를 들어 `Client.get_price_history_every_minute()` 메서드를 통해 특정 종목의 과거 1분봉 가격 데이터를 불러올 수 있습니다⁵. 이를 통해 백테스팅이나 과거 데이터 기반의 분석이 가능합니다.

### 실시간 스트리밍
Schwab API는 웹소켓 기반의 스트리밍 채널을 제공하며, schwab-py의 `StreamClient`를 사용하면 최대 실시간(up-to-the-second) 데이터 스트림에 연결할 수 있습니다⁶. 주식의 실시간 호가(Level 1), 실시간 주문장(Level 2), 체결 데이터(Time of Sales)까지 스트리밍으로 받을 수 있을 정도로 고성능입니다⁶.

특히 분 차트 데이터(OHLCV) 스트림도 제공하여, 매 분 갱신되는 시가/고가/저가/종가/거래량 데이터를 바로 수신할 수 있습니다⁷. 이를 통해 별도의 계산 없이 1분봉을 실시간 구축할 수 있으며, 해당 데이터는 Open/High/Low/Close 가격과 그 분의 거래량 정보를 포함합니다⁸. 실시간 스트리밍은 본 프로그램의 핵심인 1분봉 기반 자동거래에 활용됩니다.

### 주문 실행 및 계좌 관리
Schwab API를 통해 주문 발주 및 체결 관리가 가능합니다. schwab-py의 `Client.place_order()` 메서드를 사용하여 주식 매수/매도 주문을 자동으로 제출할 수 있으며⁹, 주문 결과로 주문 ID를 받아 추적합니다. 또한 계좌 잔고 및 포지션 조회 기능을 제공하여 실시간으로 계좌의 현금 잔고, 보유 주식 수량, 손익 등을 확인할 수 있습니다². (유의할 점: Schwab API는 현재 모의투자(Paper Trading) 계좌를 지원하지 않으므로 실제 계좌로 테스트해야 합니다¹⁰.)

### 기타 데이터 및 도구
- 기업 펀더멘털 데이터 조회: `Client.get_instruments(..., projection="fundamental")` 형태로 종목의 재무지표, 시가총액 등 펀더멘털 정보를 얻을 수 있습니다¹¹.
- 종목 검색 기능: 이름이나 심볼로 종목을 검색하는 기능도 제공합니다¹².
- 시장 Movers 정보: `Client.get_movers()`를 통해 주요 지수나 전체 시장에서 상위 변동 종목(top movers)을 가져올 수 있습니다¹³.

요약하면, schwab-py를 통한 Schwab API 연동으로 실시간 시세 수집, 과거 데이터 조회, 자동 주문 실행, 계좌정보 모니터링 등 자동매매 시스템 구축에 필요한 거의 모든 기능을 사용할 수 있습니다. API 호출 및 스트리밍 응답으로 받은 데이터는 로컬 데이터베이스에 저장/갱신하며, 이는 전략 모듈과 백테스팅에 입력 데이터로 활용됩니다.

## 시스템 주요 동작 모드 (Operational Modes)

본 프로그램은 시간대별로 3가지 모드로 구분되어 동작하며, 각 모드는 서로 다른 기능에 초점을 맞춥니다:

### 1. 종목 발굴 모드 (장 열리기 한참 전)

개장 전 한참 이른 시각 (예: 전일 야간 ~ 당일 이른 새벽)에 실행되는 모드로, 오늘 거래할 만한 유망 종목 후보를 탐색합니다.

- **데이터 스캔**: 지난 거래일의 시장 데이터를 바탕으로 큰 변동성이 있었거나 뉴스 이슈가 있는 종목을 찾습니다. 예를 들어 전일 대비 등락률 상위 종목이나 거래량 급증 종목 등을 선별합니다. Schwab API의 Top Movers 엔드포인트를 활용하면 특정 지수나 전체 주식시장에서 상승폭 상위 10개 종목 또는 하락폭 상위 10개 종목 등을 얻을 수 있습니다¹³. 이를 통해 전일 혹은 프리마켓에서 눈에 띄는 움직임을 보이는 종목을 리스트업할 수 있습니다.

- **펀더멘털/기타 필터**: 필요에 따라 기업 펀더멘털 데이터나 섹터 정보 등을 참고해 후보 종목을 거릅니다. (예: 시가총액 너무 작은 종목은 제외 등). Schwab API의 종목 검색/펀더멘털 기능을 이용하면 종목의 기초 정보를 가져와 필터링할 수 있습니다¹¹.

- **결과 저장**: 발굴된 후보 종목 리스트와 간략한 지표(전일 등락률, 거래량 등)를 DB에 저장해 두고 다음 모드에서 활용합니다. 이 모드는 자동으로 백그라운드에서 실행되며, 향후 뉴스 크롤링 데이터가 도입되면 뉴스 기반 후보추가도 이 단계에서 이뤄질 예정입니다.

### 2. 종목 선정 모드 (개장 직전)

개장 직전(프리마켓 시간대)에 실행되는 모드로, 앞 단계에서 발굴된 후보 중 실제 오늘 매매에 투입할 최종 종목들을 선정하고, 종목별 전략 및 파라미터를 세팅합니다.

- **프리마켓 모니터링**: 시장이 열리기 바로 전 시점의 호가 움직임이나 뉴스 동향을 확인하여 후보 목록을 조정합니다. 예를 들어 발굴 모드에서 선정된 종목 중 프리마켓에서 가격이 급변하거나 특별한 뉴스가 확인된 경우 우선순위를 조정하거나 제외할 수 있습니다. 실시간 시세 조회 API (`Client.get_quotes()`)를 사용하여 프리마켓 가격과 거래량을 점검합니다.

- **전략 할당**: 최종 선정된 각 종목에 대해 사용할 매매 전략을 결정합니다. 전략은 모듈화되어 있으므로, 예를 들어 모멘텀 돌파 전략, 평균회귀 전략 등 여러 전략 중 해당 종목에 적합한 것을 선택할 수 있습니다. 이 선택은 과거 데이터 분석에 기반할 수도 있고, 사용자 정의 규칙에 따라 이뤄질 수도 있습니다.

- **파라미터 최적화**: 중요 기능으로, 선정 종목의 전략별 매매 변수(파라미터)를 최적화합니다. 이를 위해 백테스팅과 AI 알고리즘을 활용합니다. 먼저 로컬 DB에 저장된 해당 종목의 과거 1분봉 시계열 데이터 (예: 최근 수일~수주의 데이터)를 불러옵니다. 그런 다음 전략별로 핵심 변수들(예: 이동평균 기간, 진입 임계값, 손절/익절 한도 등)을 변화시켜가며 백테스트를 수행합니다. 예를 들어 과거 데이터에 대해 여러 조합의 변수로 시뮬레이션하여 승률, 수익률, 최대낙폭 등의 지표를 비교합니다. 최적화는 전략 성능을 높이는 변수 값을 찾는 과정으로, 신뢰할 수 있는 과거 성과를 보인 세팅을 선택합니다¹⁴. 필요에 따라 그리드 탐색(전수검사), 유전 알고리즘 등의 자동화 기법으로 효율적으로 탐색할 수 있습니다¹⁵. 이렇게 도출된 최적 변수는 해당 종목의 그날 실시간 매매 모드에서 적용됩니다. (추후에는 강화학습 등 AI 모델을 통해 더 지능적으로 최적화를 수행하거나 시장 변화에 적응하는 기능도 고려합니다.)

- **최종 준비 출력**: 이 모드의 결과로 오늘 거래할 종목 리스트, 각 종목에 할당된 전략 모듈 이름, 초기 전략 변수 세트, 그리고 초기 자본 배분 등이 결정됩니다. 이 정보를 바탕으로 실시간 거래 모드에 진입합니다. 관련 데이터는 DB에 기록하고, GUI 상에도 오늘의 대상 종목과 주요 설정값들이 표시됩니다.

### 3. 실시간 자동 거래 모드 (시장 개장 후)

정규 장 개장 시간에 진입하면, 프로그램은 실시간 거래 모드로 전환되어 선정된 종목들의 시세를 스트리밍으로 수신하며 사전에 정한 전략에 따라 자동 매매를 수행합니다.

- **스트리밍 데이터 수집**: 개장과 동시에 schwab-py의 `StreamClient`를 이용해 선정 종목들의 실시간 데이터 스트림에 구독합니다. 여기서는 초당 틱 데이터보다는 1분마다 집계되는 분봉 데이터(OHLCV) 스트림을 활용하여 효율적으로 운용합니다⁷. 즉, 매 1분마다 해당 종목의 시가, 고가, 저가, 종가, 거래량 정보가 실시간으로 들어오며, 이를 내부적으로 1분봉 캔들 객체로 생성합니다. 새로운 1분봉이 생성될 때마다 로컬 DB에 추가 저장하여 누적 시계열을 업데이트하고, 동시에 전략 모듈에 신호 계산을 위한 입력으로 전달합니다. (만약 스트리밍 지연이나 누락이 발생할 경우를 대비해, 보조적으로 1분 단위 REST API 시세 조회를 검증 용도로 사용할 수도 있습니다.)

- **전략 신호 처리**: 각 전략 모듈은 매 분 업데이트된 데이터로 매수/매도 신호를 판단합니다. 예를 들어 모멘텀 돌파 전략이라면 특정 1분봉의 고가가 일정 범위를 돌파할 때 매수 신호를 내거나, 평균회귀 전략이라면 단기 이동평균이 장기 이동평균을 하향 이탈할 때 매도 신호를 낼 수 있습니다. 전략 모듈은 이전 분봉들로부터 계산된 지표(예: 이동평균, RSI 등)와 현재 분봉의 가격 움직임을 종합하여 실행 여부를 결정합니다. 신호가 발생하면 곧바로 다음과 같은 매매 실행 절차를 밟습니다.

- **주문 실행 및 체결 관리**: 매매 신호에 따라 자동 주문이 API를 통해 전송됩니다. `Client.place_order()`를 호출하여 시장가 주문 또는 지정가 주문을 제출하며, 미리 설정된 수량 및 주문 유형을 적용합니다⁹. 예를 들어 매수 신호 시 일정 배분금으로 해당 종목을 시장가 매수하고, 매도 신호 시 보유 포지션을 청산하는 식입니다. 주문 체결 여부 및 가격은 Schwab API로부터 확인할 수 있으며, schwab-py를 통해 계좌 활동 스트림을 구독하면 체결 정보나 취소, 정정 등의 이벤트를 실시간으로 받아올 수 있습니다¹⁶. 이를 통해 주문이 체결되면 내부 포지션 상태와 DB의 거래 기록을 즉시 업데이트하고, GUI에도 반영합니다. 만약 주문이 부분 체결되거나 실패한 경우에 대비한 예외 처리 로직도 포함합니다 (예: 주문 거부 시 재시도 또는 해당 종목 거래 중단 등).

- **리스크 관리**: 실시간 모드에서는 기본적으로 각 전략에 내재된 손절매/이익실현 규칙에 따라 움직이지만, 전반적인 리스크 통제 모듈도 고려합니다. 예를 들어 하루 총 손실 한도를 넘으면 모든 포지션 정리 후 거래를 중단하거나, 종목별 최대 포지션 사이즈를 제한하여 급변동에 대비합니다. 이러한 규칙들은 설정 가능한 파라미터로 두어, 필요시 GUI를 통해 사용자가 조정할 수 있습니다.

- **모드 종료**: 장 마감 시간이 되면 실시간 거래 모드를 종료합니다. 종료 시 오늘 실행된 모든 거래를 정리하고 일일 성과 보고서 작성을 위한 데이터를 모읍니다. 또한 미체결 주문 취소, 보유한 포지션이 있다면 전량 정리 등의 마감 처리를 자동 수행합니다.

## 전략 모듈 설계와 백테스팅/최적화 (Strategy Modules & Optimization)

본 프로그램의 알고리즘 부분은 전략 모듈화를 통해 유연성과 확장성을 갖습니다. 전략 모듈은 일정한 인터페이스를 따르는 개별 Python 클래스 또는 함수 세트로 구현되며, 데이터 입력 -> 매매신호 출력의 역할을 합니다.

### 전략 모듈 인터페이스
각 전략은 예를 들어 `Strategy` 베이스 클래스를 상속받아 `on_data_update(data)`와 같은 메소드를 구현한다고 가정합니다. `on_data_update` 메소드는 새로운 1분봉 데이터 또는 가격 틱을 인자로 받아 내부 상태(지표 계산 등)를 갱신하고, 매매 신호가 발생하면 이를 리턴하거나 콜백 형태로 상위 시스템에 알립니다. 또한 `Strategy` 클래스에는 해당 전략의 설정 파라미터들을 속성으로 갖게 하며, 예를 들어 이동평균 전략이라면 `short_window=5, long_window=20`와 같은 변수들을 가집니다. 이러한 변수는 초기 구성이나 최적화 결과를 통해 설정됩니다.

### 여러 전략 지원과 추가
초기에는 몇 가지 대표 전략으로 시작하지만, 구조적으로 신규 전략을 쉽게 추가할 수 있도록 설계합니다. 새로운 전략 클래스를 정의하고 어플리케이션에 등록만 하면, GUI를 통해 해당 전략을 종목에 할당하거나 활성화할 수 있게 할 예정입니다. 이때 각 전략은 독립적으로 플러그인처럼 동작하므로, 한 종목에 여러 전략을 멀티로 적용하는 것도 (추후 복잡한 전략 조합을 실험하기 위해) 고려할 수 있습니다.

### 백테스팅 프레임워크
전략 모듈은 실시간 모드에서만 쓰이는 것이 아니라 백테스팅 모드에서도 재사용됩니다. 즉 동일한 전략 로직을 과거 데이터에 적용하여 성능을 검증할 수 있도록 합니다. 백테스트를 위해 별도의 엔진을 두는데, DB에 저장된 과거 1분봉 데이터를 불러와 각 분봉을 차례로 `Strategy.on_data_update()`에 주입하면서 가상의 주문 실행을 시뮬레이션합니다. 이를 통해 누적 수익, 최대 낙폭, 트레이드별 승률 등의 결과를 산출합니다. 이러한 백테스트 엔진은 전략 개발/검증 단계뿐만 아니라 앞서 언급한 일일 파라미터 최적화 과정에서도 핵심적으로 사용됩니다.

### AI 기반 최적화
백테스트 엔진과 더불어, AI/머신러닝 기법으로 전략의 변수를 자동 최적화하는 기능을 도입합니다. 구체적으로, 최적화 모듈은 특정 전략과 종목에 대해 목표 함수(예: 백테스트된 총수익 또는 Sharpe Ratio 등)를 최대로 만드는 변수 조합을 찾습니다. 전통적인 완전 탐색이나 Grid Search도 가능하지만, 변수 차원이 많을 경우 Bayesian Optimization이나 유전 알고리즘 같은 기법을 적용하여 효율적으로 탐색합니다¹⁵. 또한 충분한 데이터가 축적되고 인프라가 갖춰지면, 강화학습(Reinforcement Learning)을 활용하여 에이전트가 직접 매매를 학습하도록 진화시킬 수도 있습니다. 이때도 시뮬레이션 환경으로 백테스트 엔진을 활용하게 됩니다.

### 워크플로우
일반적인 하루 워크플로우에서, 최적화는 보통 장 시작 전에 수행되어 당일에 적용될 전략 파라미터를 결정합니다. 그러나 중장기적으로는 지속적 학습 개념을 도입해, 매일 장 종료 후 그날의 거래 결과를 피드백으로 받아 머신러닝 모델을 업데이트하거나, 전략 파라미터의 워크포워드(walk-forward) 최적화를 주기적으로 실행하는 방향을 고려합니다. 이를 통해 전략이 시장 변화에 계속 적응하도록 합니다¹⁷ ¹⁸.

요약하면, 전략 모듈은 표준화된 인터페이스와 백테스팅/최적화 도구의 지원 아래 개발되며, 이를 통해 다양한 전략을 유연하게 활용하고 AI 기반의 자동 튜닝을 실현합니다. 이는 수익률 향상과 리스크 감소에 중요한 역할을 할 것으로 기대됩니다.

## 데이터 관리 및 로컬 DB 설계 (Data Management and Local DB)

자동매매 시스템에서 데이터 관리 역시 핵심입니다. 본 프로젝트에서는 로컬 데이터베이스를 구축하여 시세와 거래 내역 등 주요 데이터를 저장, 활용합니다:

### DB 선택 및 구조
경량의 관계형 DB (예: SQLite 또는 PostgreSQL 로컬 인스턴스)를 사용할 예정입니다. SQLite는 설치/운영이 간편하고 소규모 데이터에 적합하며, 초기 개발 단계에 적절합니다. DB에는 몇 가지 주요 테이블을 둡니다.

- **시세 테이블 (PriceData)**: 종목별 분봉 시계열을 저장합니다. 예를 들어 `PriceData(symbol, datetime, open, high, low, close, volume)` 구조로 ticker와 분 시각을 기본 키로 저장하며, 필요한 경우 인덱스를 설정합니다. 실시간 모드에서 매 1분봉이 완성될 때마다 해당 테이블에 INSERT되고, 추후 백테스트나 리포트용으로 활용됩니다. 또한 백필(fill) 목적으로 장 시작 전에 지난 며칠치 1분봉 데이터를 API로 가져와 채워둘 수도 있습니다 (Schwab API의 가격 히스토리 제공 범위에 따라 조정).

- **거래 내역 테이블 (Trades)**: 자동매매에 의해 발생한 매수/매도 거래를 기록합니다. 필드에는 `datetime, symbol, action(buy/sell), quantity, price, strategy_id, profit_loss` 등이 포함됩니다. 이 데이터는 일일 리포트 생성 및 전략 성과 평가에 활용됩니다.

- **전략/설정 테이블 (StrategyConfig)**: 일자별로 사용된 종목 리스트와 각 종목에 적용된 전략명, 변수 값을 저장합니다. 예를 들어 `date, symbol, strategy, params(JSON)` 등을 기록해두면, 사후에 어떤 설정으로 거래했는지 추적 가능하고 리포트에도 활용됩니다.

- **계좌 및 일별 요약 테이블**: 전체 계좌의 일별 손익, 수수료, 잔고 등의 요약치를 저장하는 테이블도 유지합니다. 이를 통해 일별/월별 성과 추이를 관리합니다.

### 데이터 수집 및 업데이트

- **실시간 수집**: 앞서 설명된 대로 스트리밍된 1분봉 데이터는 실시간으로 PriceData 테이블에 저장됩니다. 또한 주문 체결 시 Trades 테이블에 추가되고, 계좌 잔고 변동이 있을 때 계좌 테이블이 갱신됩니다. 이러한 실시간 DB 업데이트는 트랜잭션 안전성을 고려하여 이루어지며, 성능을 위해 비동기 또는 배치 처리도 검토합니다.

- **과거 데이터 적재**: 시스템 시작 시 또는 정기적(예: 매일 새벽)으로 PriceData 테이블에 누락된 과거 데이터가 있으면 채워넣습니다. Schwab API는 과거 1분봉 데이터를 요청할 수 있으므로⁵, 예를 들어 지난 10일치 1분봉을 가져와 DB 동기화합니다. 이를 통해 백테스트나 전략 분석에 충분한 데이터가 누적되도록 합니다.

### 데이터 활용
DB에 쌓인 데이터는 다양한 분석과 리포트에 활용됩니다. 백테스트 모듈은 DB에서 해당 종목의 히스토리를 불러와 사용하고, 매일 거래 종료 후 생성되는 성과 리포트도 DB의 Trades, 계좌 테이블을 집계하여 만듭니다. 또한 GUI에서 차트를 그리거나 실시간 P/L을 표시할 때도 DB의 값을 실시간 질의하거나 메모리에 캐싱된 값을 사용합니다.

### 확장성 및 안정성
초기에는 로컬 단일 DB로 운영하지만, 향후 데이터량이 커지면 분산DB나 시계열 특화DB(예: InfluxDB 등)로 이전을 검토할 수 있습니다. 또한 데이터 손상을 막기 위해 일정 주기의 백업을 수행하고, 장애 발생시 빠른 복구가 가능하도록 설계합니다.

데이터 관리의 원칙은 실시간성 확보와 이력 보존입니다. 실시간 트레이딩에서는 최신 1분 내 데이터를 신속히 처리해야 하므로 필요한 부분은 메모리에 유지하고, 백그라운드로 DB에 기록합니다. 동시에 모든 거래와 시세의 이력을 남겨두어 투명한 성과 평가와 모델 향상에 활용합니다.

## GUI 설계 (Desktop GUI for Monitoring & Control)

사용자에게는 데스크탑 GUI를 제공하여, 실시간으로 거래 상황을 모니터링하고 설정을 조정하며 결과를 확인할 수 있게 합니다. GUI는 Python 기반의 데스크탑 애플리케이션으로 구현하며, PyQt 또는 Tkinter와 같이 범용적인 GUI 프레임워크를 사용할 예정입니다 (보다 현대적 UI와 성능을 위해 PyQt5/PySide6를 권장). 주요 GUI 기능 및 디자인 요소는 다음과 같습니다:

### 실시간 시세 및 포지션 모니터
메인 화면에는 선택된 종목들의 실시간 정보가 표 형태로 나타납니다. 예를 들어 각 종목별 현재 가격, 변동률, 거래량, 현재 포지션(보유 주식 수), 실현손익 등이 갱신됩니다. 가격 정보는 Schwab API로부터 스트리밍 수신된 데이터를 그대로 표시하여 1분 단위로 업데이트됩니다.

### 차트 표시
각 종목을 클릭하면 해당 주식의 실시간 차트(캔들 차트)를 볼 수 있는 패널이나 팝업이 제공됩니다. 차트는 1분봉 캔들로 그려지며, 전략에서 사용하는 주요 기술지표(예: 이동평균선)도 함께 중첩 표시됩니다. 실시간 차트 구현을 위해선 속도가 중요한데, PyQtGraph 라이브러리를 활용하면 OpenGL 가속을 통해 Matplotlib보다 훨씬 원활한 실시간 그래픽을 구현할 수 있습니다¹⁹. PyQtGraph를 사용하여 다중 종목의 차트를 각각 탭 또는 윈도우로 나누어 띄우고, 줌/스크롤 등 상호작용도 지원합니다²⁰. 이를 통해 사용자는 실시간으로 가격 움직임과 전략 지표를 시각적으로 파악할 수 있습니다.

### 전략 및 변수 제어
GUI에서는 전략 설정을 제어할 수 있는 화면도 제공합니다. 예를 들어 오늘 거래중인 각 종목 행에 대해, 드롭다운이나 버튼으로 현재 적용된 전략명을 표시하고 이를 다른 전략으로 온더플라이 변경할 수 있게 할 수 있습니다 (변경 시에는 해당 종목의 기존 포지션 정리 등 안전장치 필요). 또한 전략의 주요 파라미터 값(예: 손절선%, 매수 임계값 등)을 조절하는 슬라이더나 입력 필드를 두어, 사용자가 실시간으로 값을 조정하고 즉각 적용되도록 할 수 있습니다. 이러한 수동 조정은 위험성이 있으므로 권고되진 않지만, 연구/튜닝 목적이나 비상 상황 대응용으로 제공됩니다. 모든 변경 사항은 프로그램 로그와 DB에 기록되어 추후 분석 시 반영합니다.

### 일일 성과 대시보드
장 종료 후 또는 사용자가 원할 때, 금일 거래 결과 요약 화면을 볼 수 있습니다. 여기에는 총 이익/손실, 승률, 거래 횟수, 수수료 등의 통계와, 종목별 개별 손익이 표시됩니다. 또한 간단한 Equity Curve(자본曲線)나 일별 손익 그래프를 차트로 그려 보여줍니다. 리포트 화면은 해당 데이터를 저장 및 내보내기 기능도 가지며, 예를 들어 PDF로 리포트 저장하거나 엑셀로 데이터 추출을 지원합니다. 이를 통해 사용자는 자신의 자동매매 성과를 일별로 축적하고 검토할 수 있습니다.

### 로그 및 알림
프로그램 실행 중의 주요 이벤트 (예: 주문 체결, 에러 발생, 전략 신호 발생 등)를 실시간으로 표시하는 로그 콘솔 영역도 GUI에 포함합니다. 에러 발생 시 팝업 알림, 특정 조건 충족 시 사용자에게 알림 소리 또는 메시지 등도 구성하여 즉각적인 피드백을 줍니다. 예를 들어 큰 손실이 발생하면 경고를 주거나, 목표 수익 달성 시 알림을 주는 식입니다.

### 사용자 조작 요소
GUI 상단에는 시작/중지 버튼이 있어 전체 자동매매의 동작을 사용자 제어로 일시 정지하거나 재개할 수 있습니다. 또한 종목 추가/삭제 기능도 제공하여, 장 중에 긴급히 종목을 제외하거나 (또는 수동 개입으로 추가 매매하고자 하는 경우) 사용할 수 있게 합니다. 다만 이러한 수동介入은 백테스트나 통계에 어긋날 수 있으므로 관리자 권한 기능으로 제한할 수 있습니다.

### UX 고려사항
GUI는 데스크탑 애플리케이션으로서 실시간성이 생명인 만큼, 쓰레드 분리와 성능 최적화에 신경씁니다. 트레이딩 로직과 데이터 수신은 별도 쓰레드/프로세스로 돌리고, GUI 스레드는 화면 렌더링에 집중하게 설계합니다. PyQt의 경우 시그널/슬롯 메커니즘을 활용하여 백엔드에서 온 데이터 업데이트를 GUI에 안전하게 전달합니다. 또한 GUI 프레임 업데이트 주기는 1초 이내로 빠르게 하여 사용자에게 부드러운 실시간 경험을 제공합니다.

### 테마 및 레이아웃
직관적인 인터페이스를 위해 다중 패널을 가지는 대시보드 형태로 구성하고, 어두운 배경(Dark theme)을 적용하여 장시간 화면을 보아도 눈의 피로를 줄이도록 합니다. 각 구성 요소는 dock이나 tab으로 배치하여 사용자가 원하는 정보만 골라 볼 수도 있게 유연성을 줍니다.

요약하면, GUI는 실시간 모니터링 대시보드와 제어센터 역할을 합니다. 사용자는 이를 통해 알고리즘 거래 과정을 한 눈에 파악하고, 필요시 제한적으로 개입하거나 설정을 손볼 수 있습니다. 또한 거래 완료 후 결과를 손쉽게 검토하고 축적된 자료를 확인할 수 있어 사용자 경험 측면에서도 만족도를 높입니다.

## 추후 뉴스 데이터 통합 계획 (Future Integration of News Data)

향후 확장 기능으로 뉴스/외부 정보의 활용을 고려하고 있으며, 이를 염두에 두고 시스템을 설계합니다. 금융 뉴스나 공시, SNS 정보 등 비정형 데이터를 알고리즘에 결합함으로써 정량적 지표 외에 정성적 요인까지 반영하는 것이 목표입니다.

### 뉴스 수집 방식
비교적 간단한 방법으로는 RSS 피드 크롤링을 들 수 있습니다. 주요 금융뉴스 사이트(예: Yahoo Finance, Reuters, 한글 뉴스 등)의 RSS 피드를 주기적으로 읽어 최신 뉴스를 수집합니다. 이를 종목 티커와 매핑하여, 현재 관심 종목과 관련된 뉴스만 걸러내는 작업이 필요합니다. 예를 들어 특정 종목명이 헤드라인에 포함되어 있거나, 업종 전반에 영향 있는 거시경제 뉴스 등을 선별합니다. RSS 외에도 뉴스 API(예: Alpha Vantage 뉴스 API 등)나 소셜 미디어 트렌드 API를 활용할 수도 있습니다. 이러한 외부 데이터 수집기는 별도 모듈로 구현하여, 메인 트레이딩 로직과 분리된 프로세스로 동작하게 합니다.

### 텍스트 분석 및 변수 생성
수집된 뉴스 텍스트는 자연어 처리를 통해 유용한 변수로 변환됩니다. 예를 들어 감성 분석(Sentiment Analysis) 모델을 적용해 뉴스가 해당 종목에 긍정/부정 영향을 줄 지 점수화하거나, 중요 키워드를 추출하여 이벤트 발생 여부를 파악할 수 있습니다. 이러한 분석 결과를 정량적인 숫자 변수로 만들어 전략 모듈에 입력합니다. (예: 뉴스 감성점수 +5 또는 -5를 해당 종목의 전략에 추가 인자로 제공).

### 전략과의 통합
전략 모듈 쪽에서는 뉴스로부터 나온 추가 변수를 입력으로 받을 수 있도록 인터페이스를 확장합니다. 예를 들어 `on_data_update(data, news_score=None)` 형태로 추가 인자를 허용하고, 만약 특정 종목에 매우 부정적인 뉴스가 나온 경우 매수 신호를 무시하거나 포지션 사이즈를 줄이는 식의 로직을 넣을 수 있습니다. 혹은 반대로 호재 뉴스 때 적극 매수하는 뉴스 트레이딩 전략도 모듈로 추가할 수 있습니다.

### 설계 고려
아직 뉴스 연계 기능을 구현하진 않지만, 확장성을 위해 현재 시스템 설계 단계에서 이를 고려합니다. 예를 들어 DB 구조에 종목별 일자별 뉴스 감성 점수 필드를 추가로 마련해 둘 수 있습니다. 또 GUI에도 추후 뉴스 헤드라인을 표시할 수 있는 위젯 공간을 염두에 둡니다. 모드 상으로는, 뉴스 데이터는 종목 발굴 모드나 선정 모드에서 활용될 가능성이 높습니다 (핵심 뉴스가 나온 종목을 포착하여 거래 후보로 삼거나 제외하는 용도). 실시간 모드에서도 중대한 뉴스 속보는 급변동을 일으킬 수 있으므로 이를 리스크 관리 트리거로 쓸 수 있습니다.

### 모델 고도화
장기적으로는 뉴스뿐 아니라 재무 지표, 경제 지표 일정, 대체 데이터 등 여러 데이터를 통합한 멀티팩터 AI 모델을 개발할 수 있습니다. 이러한 모델은 매일 아침 종목 스코어링을 통해 어떤 종목을 거래할지, 포지션 사이즈는 얼마나 할지까지 의사결정에 참여하게 할 수 있습니다. 이러한 부분까지 고려하면 시스템은 단순한 규칙 기반 알고리즘에서 학습 기반의 적응적 시스템으로 발전하게 됩니다.

현재 단계에서는 뉴스 통합을 설계상의 고려사항으로 두고, 실제 구현은 추후 진행할 예정입니다. 그러나 위와 같이 시스템 각 부분이 뉴스 데이터 접목을 수용할 수 있도록 모듈화와 유연성을 확보해 둘 것입니다. 새로운 데이터 소스를 쉽게 플러그인하고, 해당 데이터를 전략이나 의사결정에 반영할 수 있는 확장형 아키텍처를 지향합니다.

## 결론 (Conclusion)

이상으로 자동 주식매매 프로그램의 기능 명세와 설계 방향을 정리했습니다. Charles Schwab의 강력한 API를 기반으로 실시간 데이터 수집부터 주문 실행까지 자동화하고, 1분봉 기준의 알고리즘 트레이딩을 구현합니다. 3단계 모드 운영으로 시장 상황에 선제적으로 대응하며, 전략 모듈화와 AI 최적화 기능으로 지속적으로 성능을 향상시킵니다. 사용자용 데스크탑 GUI를 통해 투명하고 편리한 모니터링/제어 환경을 제공하고, 향후 뉴스 데이터 연계까지 고려한 확장성을 확보했습니다. 이 PRD를 바탕으로 향후 개발 단계에서는 각 모듈의 구체적인 구현과 테스트를 진행하고, 실제 시장 데이터에 대한 시뮬레이션을 통해 시스템을 고도화할 것입니다. 목표는 안정적이고 수익성 있는 자동매매 시스템을 구축하는 것이며, 이를 위해 지속적인 개발과 개선을 이어나갈 것입니다.

출처: 본 문서에서는 Charles Schwab API 및 schwab-py 라이브러리의 공식 문서 내용을 참고하여 기능을 기술하였으며¹ ² ¹⁰, PyData 컨퍼런스 사례를 참고하여 실시간 시스템 및 GUI 설계에 아이디어를 얻었습니다¹⁹ ²⁰. 전략 최적화에 관한 일반적인 내용은 알고리즘 트레이딩 분야의 공개 자료를 참고하여 작성되었습니다¹⁴ ¹⁵. 각주에 명시된 번호는 해당 출처의 일부분을 인용한 것입니다.

---

### 참조 문헌

1. schwab-py · PyPI  
   https://pypi.org/project/schwab-py/

2. schwab-py · PyPI

3. Transitioning from TDAmeritrade and tda-api — schwab-py documentation  
   https://schwab-py.readthedocs.io/en/latest/tda-transition.html

4. Transitioning from TDAmeritrade and tda-api — schwab-py documentation

5. HTTP Client — schwab-py documentation  
   https://schwab-py.readthedocs.io/en/latest/client.html

6. Streaming Client — schwab-py documentation  
   https://schwab-py.readthedocs.io/en/latest/streaming.html

7. Streaming Client — schwab-py documentation

8. Streaming Client — schwab-py documentation

9. HTTP Client — schwab-py documentation

10. schwab-py · PyPI

11. HTTP Client — schwab-py documentation

12. HTTP Client — schwab-py documentation

13. HTTP Client — schwab-py documentation

14. Trading Strategy Optimization: Example, Definition, Backtesting - QuantifiedStrategies.com  
    https://www.quantifiedstrategies.com/trading-strategy-optimization/

15. Trading Strategy Optimization: Example, Definition, Backtesting - QuantifiedStrategies.com

16. Streaming Client — schwab-py documentation

17. Trading Strategy Optimization: Example, Definition, Backtesting - QuantifiedStrategies.com

18. Trading Strategy Optimization: Example, Definition, Backtesting - QuantifiedStrategies.com

19. PyData Madrid 2016 | Presentation: Python for developing a real-time automated trading platform  
    https://pydata.org/madrid2016/schedule/presentation/11/

20. PyData Madrid 2016 | Presentation: Python for developing a real-time automated trading platform